# no cap, we don't want these files fr fr 💀
node_modules/

# Python virtual environments (venv said "it's giving bloat energy")
venv/
.venv/
env/
.env/
pyvenv.cfg
__pycache__/
*.py[cod]
*.py[co]
*.pyo
*.pyd
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
*.whl

# Jupyter Notebook checkpoints (nobody asked for these bestie)
.ipynb_checkpoints

# pyenv (Python version management is sus)
.python-version

# pipenv (package management but make it complicated)
Pipfile.lock

# poetry (for the aesthetic developers)
poetry.lock

# Celery stuff (task queue? more like task queue deez nuts)
celerybeat-schedule
celerybeat.pid

# SageMath parsed files (math is hard okay)
*.sage.py

.idea/
dist/

# macOS being extra as usual 💅
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# logs (because debugging is pain and suffering)
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# Runtime data (nobody cares about your PID files bestie)
pids
*.pid
*.seed
*.pid.lock

# Coverage directory used by tools like istanbul (testing? we don't do that here)
nyc_output/

# Grunt intermediate storage (grunt work am I right?)
.grunt

# Bower dependency directory (bower? in 2024? bestie no...)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (https://nodejs.org/api/addons.html)
build/Release

# Dependency directories (the forbidden folders)
node_modules/
jspm_packages/

# Snowpack dependency directory (https://snowpack.dev/)
web_modules/

# TypeScript cache (TypeScript said "trust me bro")
*.tsbuildinfo

# Optional npm cache directory (cache money records who?)
.npm

# Optional eslint cache (linting is just suggestions anyway)
.eslintcache

# Microbundle cache
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variables file (secrets are for keeping)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
# AI/ML stuff (the robots are taking over fr)
.playwright-mcp/
.vercel
api/\[domain\]/v1/\[rpc\].js
api/\[\[...path\]\].js
.claude/
.cursor/
CLAUDE.md
.env.vercel-backup
.env.vercel-export
.agent/
.factory/
.windsurf/
skills/
ideas/
docs/internal/
internal/
test-results/
src-tauri/sidecar/node/*
!src-tauri/sidecar/node/.gitkeep

# AI planning session state (when the AI has an existential crisis)
.planning/

# Compiled sebuf gateway bundle (built by scripts/build-sidecar-sebuf.mjs)
api/[[][[].*.js
.claudedocs/

# Large generated data files (these files said "I'm not like other files, I'm chunky")
scripts/data/pizzint-processed.json
scripts/data/osm-military-processed.json
scripts/data/military-bases-final.json
scripts/data/dedup-dropped-pairs.json
scripts/data/pizzint-partial.json
scripts/data/gpsjam-latest.json
scripts/data/mirta-raw.geojson
scripts/data/osm-military-raw.json

# Iran events data (sensitive, not for public repo) - this one hits different 😳
scripts/data/iran-events-latest.json

# Military bases rebuild script (references external Supabase URLs) - Pentagon wants to know your location
scripts/rebuild-military-bases.mjs
.wrangler

# Runtime artifacts (generated by sidecar/tools, not source code) - the aftermath of our crimes
api-cache.json
verbose-mode.json
skills-lock.json
tmp/

# Extra sauce (just in case we missed something)
# IDEs and editors being annoying as usual
.vscode/
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.swp
*.swo
*~

# OS generated files (why do operating systems make so much trash?)
*.tmp
*.temp
*.bak
*.backup
*.old
*.orig

# Rust stuff (blazingly fast at creating cache files)
target/
Cargo.lock

# Go stuff (gopher noises)
vendor/
*.exe
*.exe~
*.dll
*.so
*.dylib

# Java stuff (verbose as always)
*.class
*.jar
*.war
*.ear
*.zip
*.tar.gz
*.rar

# Database files (persistent little gremlins)
*.sqlite
*.sqlite3
*.db
*.db3
*.s3db
*.sl3

# This .gitignore file is serving looks and protecting the repo 💅✨
# No cap, we're keeping it clean and organized bestie!
